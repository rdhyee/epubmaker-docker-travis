language: python
python:
  - "2.7"
sudo: required

before_install:
- sudo apt-get install -y python-lxml libxml2-dev libxslt1-dev git tidy
- pip install --use-mirrors pillow cssutils lxml docutils roman
- pip install --use-mirrors future requests

# to do: kindlegen

install:
- pip install git+git://github.com/rdhyee/pg-epubmaker.git

script:  python test.py

branches:
  only:
  - master
